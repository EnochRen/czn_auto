---
description: 项目结构与架构概览（CZN Zero Farm 卡厄思梦境零式自动刷取脚本）
alwaysApply: true
---

# 项目概览

**CZN Zero Farm** 是《卡厄思梦境》PC 端自动化刷取脚本。纯 OpenCV 模板匹配 + 状态机驱动，无 AI 模型 / 无 GPU，仅支持 Windows，基于 1920×1080 分辨率坐标（按比例缩放）。

# 模块结构

| 文件 | 职责 |
|---|---|
| `main.py` | CLI 主入口。`Config` 加载配置、日志、热键、主循环 `main_loop`、状态分发 `handle_state`、`--capture` 模板采集模式 |
| `core/gui/` | PySide6 + Fluent（qfluentwidgets）GUI 包，GUI 主入口（双击 `run.bat` 或 `python -m core.gui` 启动）。窗口外壳/导航用 Fluent，内容区为纯 Qt 控件 + 集中式扁平主题（简约黑夜模式：近黑中性灰画布 + 克制单色交互，主按钮近白、选中/聚焦走中性灰，仅日志告警/错误与状态点保留功能色）。按职责拆分：`app.py`（DPI/QApplication 入口 `run()`）、`__main__.py`（`python -m core.gui` 入口）、`main_window.py`（`MainWindow` Fluent 导航、工作线程/日志/热键统筹）、`theme.py`（`Palette` 调色板 + `build_stylesheet`/`apply_theme` 全局扁平 QSS，关闭 Mica）、`worker.py`（`AutomationWorker(QThread)` 状态机主循环 `run` + 零式分发 `_dispatch_state` + 赛季图 `_handle_season_reroll`，经 Qt 信号回传统计）、`tools.py`（`CaptureService` 模板采集 / `DiagnoseService` 诊断 / `grab_game_frame` 单次抓帧供调试页）、`config_manager.py`（`ConfigManager` 读写 `config.json`）、`logging_bridge.py`（`QtLogHandler` 把日志经信号送 UI）、`constants.py`（路径/profile/下拉映射）、`assets/`（主题用 SVG 等资源）、`pages/`（`HomePage` 运行页（含「目标窗口」下拉 + 刷新：经 `core.window.discover_windows` 枚举候选、选中后 `select_window` 存入进程全局，不写 config） + `SettingsPage` 设置页 + `DebugPage` 开发调试页：`QTabWidget` 切换调试功能，首个 Tab `PixelDebugTab` 调试像素点——`_FrameGrabber(QThread)` 即时抓帧并经 `diagnose_frame` 识别状态/逐条像素规则校验，`ClickableImageView` 等比缩放显示、点击采样 RGB/相对坐标并叠加规则点通过/不通过标记） |
| `core/screencap/` | 屏幕捕获包：`capturer.py`（`ScreenCapturer` 门面，按 `capture_method` 选择捕获后端，窗口/设备定位、裁剪与分辨率缩放，`set_device` 绑定 ADB）、`base.py`（抽象基类 `ScreencapBackend`，含 `returns_window_only`/`is_device` 标志）、`dxgi.py`（dxcam/DXGI 桌面复制，前台）、`framepool.py`（WinRT FramePool，后台/遮挡可截，依赖 `windows-capture`）、`printwindow.py`（GDI PrintWindow，后台/遮挡可截）、`adb.py`（`AdbScreencapBackend`，`is_device=True`，经 `core.adb.screencap` 抓安卓设备整屏并缩放到基准分辨率）、`__init__.py`（`create_backend` 工厂 + `AVAILABLE_METHODS` + 重新导出 `ScreenCapturer`） |
| `core/input/` | 输入模拟包（后端分层）。`base.py` 定义 `InputMethod` 枚举与 `InputBackend` 抽象基类；`sendinput.py`/`sendmessage.py`/`postmessage.py`/`adb.py` 四个后端实现；`simulator.py` 为门面 `InputSimulator`（兼容旧 API，`set_device` 绑定 ADB）；`__init__.py` 提供 `create_backend(method)` 工厂与 `AVAILABLE_METHODS`。四种后端：`sendinput`（前台/真实鼠标）、`sendmessage`（后台/仍移动光标）、`postmessage`（后台/不碰鼠标，按客户区坐标投递并解析子窗口）、`adb`（安卓设备/经 `core.adb.tap` 点击，按设备像素，不依赖窗口句柄） |
| `core/adb/` | ADB 调用底座：`client.py`（`adb_path` 定位内置 `bin/adb/adb.exe`，开发态项目根、打包态优先 `sys._MEIPASS`，缺失兜底 PATH；`list_devices`/`screencap`(→BGR ndarray)/`tap` 封装 subprocess，隐藏窗口 + 超时；`connect_emulators` 并发探测 `DEFAULT_EMULATOR_PORTS`(5550-5585 + MuMu/MEmu/Nox 等默认端口) 并自动 `adb connect`，`list_devices(connect_emulators_first=True)` 先连后枚举）、`__init__.py`（重新导出上述函数）。被 `core/window`(设备发现)、`core/input`(点击)、`core/screencap`(截屏) 三处复用 |
| `core/matcher/` | 模板匹配 + 状态检测包，按单一职责拆分：`states.py`（`GameState` 枚举，49 个状态）、`template.py`（`TemplateMatcher` 模板匹配，阈值 0.8，不感知状态语义）、`pixel.py`（`load_pixel_checks` 加载像素规则 + `PixelChecker`/`PixelRule`/`PixelPoint` 多点 RGB 校验）、`checks.py`（`STATE_CHECKS` 有序检测优先级表 + `TEMPLATE_THRESHOLDS` 阈值数据）、`detector.py`（`StateDetector` 把模板/像素按 `before` 优先级编排成状态识别）、`__init__.py`（重新导出 `GameState`/`TemplateMatcher`/`StateDetector`/`load_pixel_checks`，对外 API 与原 `detector` 模块一致） |
| `core/window/` | 目标窗口/设备发现包（面向对象，便于接入模拟器/ADB）。**目标窗口不进 config**：固定游戏标题写在代码里，用户每次启动在首页选一次，作为**进程内全局状态**。`base.py`（`WindowTarget` 数据类含 `hwnd`/`title`/`provider_key`/`provider_label`/`device_id` + 抽象基类 `WindowProvider` + `enum_top_windows`/`get_window_title`/`find_window_by_title` 工具）、`game.py`（`GameWindowProvider` 发现游戏本体窗口，匹配「卡厄思梦境/卡厄思夢境/Chaos Zero Nightmare」三种标题）、`adb.py`（`AdbDeviceProvider` 经 `core.adb.list_devices(connect_emulators_first=True)` 先自动连接常见模拟器端口再发现安卓设备/模拟器，`hwnd=0`、`device_id=serial`）、`__init__.py`（提供者注册表 `register_provider`/`available_providers` + 跨提供者枚举 `discover_windows`(按 `provider_key/hwnd/device_id` 去重) + 进程全局选择 `select_window`/`get_selected`/`get_selected_device` + 解析句柄 `resolve_hwnd`）。首页「目标窗口」下拉与 `worker`/`tools`/`input.simulator` 统一调 `resolve_hwnd`（ADB 走 `get_selected_device`）|
| `core/combat.py` | `CombatModule`：战斗自动出牌逻辑（轮廓分析识别手牌），坐标/时序均来自 `config` |
| `core/keepawake.py` | `KeepAwake`：运行时防锁屏/防休眠（Windows `SetThreadExecutionState`），由 `config.json` 的 `game.prevent_lock` 控制，在状态机线程内 `enable()`/`disable()` |
| `core/ocr/` | OCR 后端包（后端分层，同 `core/input`/`core/screencap`）：`base.py`（抽象基类 `OcrBackend` + 共享 `find_text`，`TextBox` 类型）、`windows.py`（`WindowsOcrBackend` WinRT 系统 OCR）、`paddle.py`（`PaddleOcrBackend` PaddleOCR）、`reader.py`（门面 `OcrReader`）、`__init__.py`（`create_backend(backend, lang)` 工厂 + `AVAILABLE_BACKENDS` + 重新导出 `OcrReader`）。用于「赛季图初始刷取」模式按关键词刷 Buff |
| `config.json` | 全部运行配置：坐标点、点击偏移、时序、OCR 关键词、模板 profile 等 |
| `templates/` | 模板与像素规则统一根目录：`templates/templates_cn/`、`templates/templates_global/` 为国服 / 国际服模板图片（PNG），由 `config.json` 的 `template_profile` 切换；`templates/templates_colors/` 为像素点判断规则（JSON），`core/matcher/pixel.py` 的 `load_pixel_checks()` 优先读 `<profile>.json`，回退 `pixel_checks.json` |
| `packaging/` | 打包相关文件：`czn_auto.spec`（PyInstaller 配置，用 `SPECPATH` 解析项目根，路径与运行目录无关）、`build.bat`（一键打包，自动 `cd` 到项目根、产物输出根目录 `dist/`、复制 `config.json`、整个 `templates/` 与 `bin/`）、`app_icon.ico`（exe 本体图标 + 运行时窗口/任务栏图标，经 spec 的 `datas`/`icon` 打入） |
| `bin/adb/` | 内置 ADB 二进制目录：放 `adb.exe` + `AdbWinApi.dll` + `AdbWinUsbApi.dll`（不入 git，需自行下载）。`core/adb/adb_path()` 优先调此处，缺失兜底 PATH；`build.bat` 将整个 `bin/` 复制到 exe 同目录 |

# 核心架构

- **状态机循环**：GUI 主入口为 `core/gui/worker.py` 的 `AutomationWorker.run`（QThread）截屏 → `StateDetector.detect` 识别 `GameState` → `_dispatch_state` 根据状态点击对应 `click_points`；`main.py` 的 `main_loop`/`handle_state` 为 CLI 子集。GUI 与工作线程通过 Qt 信号通信（统计回传、日志经 `QtLogHandler`），UI 不被工作线程直接触碰。
- **节点判断**：以**有序模板匹配**为主（`core/matcher/checks.py` 的 `STATE_CHECKS` 列表越靠前优先级越高，第一个命中即返回）；另支持 **像素点判断**（规则放 `templates/templates_colors/`，经 `load_pixel_checks()` 读取，`PixelChecker` 校验）——相对坐标 `rx/ry`、RGB 每通道默认 ±10 容差、一个 check 可多点 `all`/`any`、`before` 指定插入优先级，命中后写 `last_pos/last_template` 与模板路径一致。`StateDetector` 仅做编排，匹配算法在 `TemplateMatcher`/`PixelChecker`。详见 `docs/节点判断机制.md`。
- **捕获后端**：`ScreenCapturer` 不实现截图算法，按 `config.json` 的 `game.capture_method`（`auto`/`framepool`/`printwindow`/`adb`，枚举 `core.screencap.CaptureMethod`，默认 `framepool`）经 `create_backend` 选择后端。桌面型后端（DXGI）抓整屏后按整窗口矩形裁剪再缩放到基准分辨率；窗口型后端（FramePool/PrintWindow，`returns_window_only=True`）抓整窗口后裁剪出客户区（不缩放，去标题栏/边框），支持窗口被遮挡/后台时截图；设备型后端（ADB，`is_device=True`）抓安卓设备整屏后直接缩放到基准分辨率。`game_to_screen` 坐标换算对桌面型用整窗口矩形、对窗口型用客户区矩形、对设备型按设备实际分辨率把基准坐标映射回设备像素。
- **坐标驱动**：所有点击坐标来自 `config.json` 的 `click_points` / `click_offsets`，基于 1920×1080，运行时按实际分辨率缩放。修改行为优先改配置而非硬编码坐标。
- **目标窗口发现**：不再各处直接 `FindWindowW`，也**不经 config**，统一经 `core.window`。提供者 `WindowProvider.discover()` 枚举一类候选（`GameWindowProvider` 游戏窗口、`AdbDeviceProvider` ADB 设备，未来加模拟器各一个 provider），`discover_windows()` 汇总供首页下拉；用户选中即 `select_window()` 存入进程全局。运行期 `resolve_hwnd()` 解析句柄：全局选择句柄仍有效则直接用 → 句柄失效（窗口重开）按标题(+提供者)重定位 → 从未选择则兜底取第一个候选。ADB 设备无句柄，运行期改用 `get_selected_device()` 取 serial。`worker`/`tools`/`input.simulator` 均按是否有 serial 分流（有则 `set_device`，否则 `resolve_hwnd`+`set_window`）。
- **ADB 自动切换**：首页「目标窗口」选中 ADB 设备时，`HomePage._apply_methods_for_target` 自动把 `game.input_backend`/`capture_method` 切为 `adb`（先把原 PC 值记到 `game.prev_input_backend`/`prev_capture_method`），切回 PC 窗口时从记忆值恢复；变更经 `methodsChanged` 信号通知设置页 `sync_methods` 刷新下拉。
- **两种模式**（`config.json` 的 `game.mission`）：`zero_system`（完整刷层状态机）与赛季图 OCR 刷 Buff。
- **热键**：F6 开始 / F8 停止 / F9 暂停（需管理员权限）。

# 约定

- 坐标格式：`[x, y]` 像素；区域格式：`[x, y, w, h]`。
- 新增游戏画面识别：在 `core/matcher/states.py` 的 `GameState` 加枚举 → `templates/templates_*/` 加模板图 → `core/matcher/checks.py` 的 `STATE_CHECKS` 按优先级加 `(模板名, GameState)`（需要时在 `TEMPLATE_THRESHOLDS` 配阈值）→ `core/gui/worker.py` 的 `_dispatch_state` 加分支 → `config.json` 加坐标。
- GUI 用 PySide6 + qfluentwidgets：按职责放进 `core/gui/`，遵循面向对象；跨线程更新 UI 一律走 Qt 信号；新增配置项在 `pages/settings_page.py` 读写、必要时在 `constants.py` 加映射。
- GUI 视觉为简约扁平黑夜模式（近黑中性灰、低饱和）：颜色/字体集中在 `theme.py`（`Palette` + `FONT_FAMILIES` + 全局 QSS），字体优先 Noto Sans 再回退微软雅黑。新增控件优先用纯 Qt 控件并复用 `objectName`（如 `card`/`pageTitle`/`sectionTitle`/`fieldLabel`/`statValue`/`logView`/`segmented`/`segment`）或 `kind` 属性（`primary`/`danger`/`ghost`）套用既有样式，不要在控件上散写内联样式，避免引入彩色（功能色仅限日志告警/错误与状态点）。
- 像素点判断规则写 `templates/templates_colors/<profile>.json`（回退 `pixel_checks.json`），**不写进 `config.json`**：每条规则含 `state`（GameState 值）、`points`（`rx`/`ry` 相对坐标 + `rgb` RGB 目标色 + 可选 `tol`，默认 10）、`mode`（`all`/`any`）、可选 `click`(`[rx,ry]`)、可选 `before`(模板名)。坐标用相对值天然适配各分辨率。
- 新增输入后端：在 `core/input/base.py` 的 `InputMethod` 加枚举 → 新建后端文件继承 `InputBackend` 并实现 `click()` → 在 `core/input/__init__.py` 的 `create_backend` / `AVAILABLE_METHODS` 注册。
- 新增目标窗口来源（如模拟器）：新建 `core/window/<brand>.py` 继承 `WindowProvider`、声明 `key`/`label` 并实现 `discover()`（用 `enum_top_windows` 过滤标题/类名）→ 在 `core/window/__init__.py` 的 `_PROVIDERS` 注册；首页下拉与 `resolve_hwnd` 自动纳入，无需改业务代码。
- 新增 OCR 后端：新建后端文件继承 `core/ocr/base.py` 的 `OcrBackend` 并实现 `scan()`（`find_text` 复用基类）→ 在 `core/ocr/__init__.py` 的 `create_backend` / `AVAILABLE_BACKENDS` 注册。
- 代码注释与日志混用中英文，保持现有风格；面向用户的文档用简体中文。
